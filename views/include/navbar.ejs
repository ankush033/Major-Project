<nav class="navbar navbar-expand-md bg-white shadow-sm border-bottom sticky-top py-2">
  <div class="container-fluid">
    <!-- Brand -->
    <a class="navbar-brand fs-3 text-danger" href="/listings">
      <i class="fa-regular fa-compass"></i>
      <span class="fw-semibold ms-1">Explore</span>
    </a>

    <!-- Toggle button -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
      data-bs-target="#navbarNav" aria-controls="navbarNav"
      aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Navbar content -->
    <div class="collapse navbar-collapse" id="navbarNav">
      <!-- Center search -->
      <form class="d-flex mx-auto search-bar" method="GET" action="/listings/search"
        style="max-width: 500px; width: 100%;">
        <input class="form-control me-2 rounded-pill px-3"
          type="search" name="query"
          placeholder="Search destinations" aria-label="Search" />
        <button class="btn btn-danger rounded-pill px-4" type="submit">
          <i class="fa-solid fa-magnifying-glass"></i>
        </button>
      </form>

      <!-- Right side -->
      <div class="navbar-nav ms-auto align-items-center">
        <a class="nav-link" href="/listings/new">Airbnb your home</a>
        <% if (!currentUser) { %>
          <a class="nav-link fw-semibold" href="/signup">Signup</a>
          <a class="nav-link fw-semibold" href="/login">Login</a>
        <% } else { %>
          <a class="nav-link fw-semibold" href="/logout">Logout</a>
        <% } %>
      </div>
    </div>
  </div>
</nav>
